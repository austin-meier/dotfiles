{
   "name": "dotfiles-install",
   "private": true,
   "type": "module",
   "description": "Cross-platform installer for ~/.config. Runs on Node's type stripping, no build step and no runtime dependencies.",
   "scripts": {
      "test": "node --test",
      "typecheck": "tsc --noEmit"
   },
   "devDependencies": {
      "@types/node": "^24.0.0",
      "typescript": "^5.9.0"
   }
}
